// Tema 6: If y else if

// Uso de condiciones para tomar decisiones.

let nota = 17;

// Si la nota es mayor o igual a 19
if (nota >= 19) {

    // Mostrar mensaje Excelente
    console.log("Excelente");

// Si la nota es mayor o igual a 13
} else if (nota >= 13) {

    // Mostrar mensaje Aprobado
    console.log("Aprobado");

    // Si no cumple ninguna condición
} else {

    // Mostrar mensaje Desaprobado
    console.log("Desaprobado");

}