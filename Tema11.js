// Tema 11: Do While

// Uso del ciclo do while para ejecutar instrucciones al menos una vez.
let numero = 10;

// Ejecutar primero el bloque de código:
do {
    // Mostrar número en consola:
    console.log(numero);
    // Evaluar condición:
} while (numero <= 5);